// fixme: do these need to be global? maybe under the relevant directory

export enum EntityStatus {
  created = 1,
  modified = 2,
  accessed = 3,
  deleted = 4,
}

export const SpecialConstants = {
  detailsPaneHeaderHeight: '4.6rem',
}
