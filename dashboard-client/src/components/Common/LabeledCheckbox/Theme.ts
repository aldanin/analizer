export interface ThemeProps {
  checkbox: {
    bgColor: string,
    textColor: string,
    borderColor: string,
  },
}

export const defaultTheme = {
  checkbox: {
    bgColor: 'white',
    textColor: 'black',
    borderColor: 'gray',
  },
}
