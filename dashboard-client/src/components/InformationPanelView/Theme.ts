export interface ThemeProps {
  textColor: string,
  activeTab: string;
  activeBorder: string;
}

export const defaultTheme = {
  textColor: 'black',
  activeTab: 'white',
  activeBorder: 'lightskyblue',
}
