export interface ThemeProps {
  textColor: string;
  linkColor: string;
  pipeColor: string;
  titleColor: string;
  borderColor: string;
  subTextColor: string;
  expandBgColor: string;
}

export const defaultTheme = {
  textColor: 'black',
  linkColor: '#627892',
  pipeColor: '#CED4E8',
  titleColor: '#627892',
  borderColor: '#E9EDF6',
  subTextColor: '#a9a9a9',
  expandBgColor: '#f2f4fa',
}
